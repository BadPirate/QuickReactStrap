import React from 'react';
import './App.css';

export default () =>
(
  <div className="App">
    <header className="App-header">
      <p>
        Edit <code>.env</code> and package.json to get going.
      </p>
    </header>
  </div>
)
